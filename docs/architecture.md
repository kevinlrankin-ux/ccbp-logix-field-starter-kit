## Architecture Notes (Rockwell Mirror)

This repo mirrors the Siemens template, adapted to Rockwell Logix conventions.

Key mapping:
- Siemens DBs ? Logix controller tags / UDT instances
- Siemens FBs ? Logix AOIs
- OB1 ? MainRoutine / task routine
- HMI policy: NO_INST_DB ? NO_AOI_INTERNALS
